import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create({
    container: {
        top: 0,
        bottom: 0,
        left: 0,
        right: 0,
        alignItems: "center",
        justifyContent: "center",
        position: "absolute",
        padding: 16,
    },
    label: {
        fontWeight: "normal",
        marginBottom: 8,
        textAlign: "center",
    },
    childrenContainer: {
        paddingTop: 8,
    },
});